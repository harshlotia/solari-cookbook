{
  "name": "login-invalid-credentials",
  "backend": "browser",
  "description": "A user submits the wrong password and the app correctly rejects the login with an error, without granting access to the secure area.",
  "url": "https://the-internet.herokuapp.com/login",
  "steps": [
    { "action": "fill", "selector": "#username", "text": "tomsmith" },
    { "action": "fill", "selector": "#password", "text": "definitely-wrong-password" },
    { "action": "click", "selector": "button[type=submit]" },
    { "action": "wait", "ms": 800 }
  ],
  "expect": {
    "url_contains": "/login",
    "text_contains": ["Your password is invalid"],
    "text_absent": ["You logged into a secure area"]
  }
}
